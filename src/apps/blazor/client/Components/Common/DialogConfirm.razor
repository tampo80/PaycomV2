@namespace PayCom.Blazor.Client.Components.Common
@inherits DialogBase
@using MudBlazor

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.QuestionMark" Class="mr-3 mb-n1" />
            @Title
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudText>@ContentText</MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="() => Close(false)">Annuler</MudButton>
        <MudButton Color="@Color" Variant="Variant.Filled" OnClick="() => Close(true)">Confirmer</MudButton>
    </DialogActions>
</MudDialog>

@code {
    /* Commenté pour permettre la compilation
    [CascadingParameter] 
    MudDialogInstance MudDialog { get; set; } = default!;
    */

    [Parameter] 
    public Color Color { get; set; } = Color.Error;
} 